# Dedicated, disposable Odoo instance — the SECOND accounting engine, for cross-validating
# ERPNext's ground-truth postings. Isolated: own project (odoogen), port 8101, own volumes.
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
    volumes:
      - odoo-db:/var/lib/postgresql/data
  odoo:
    image: odoo:17
    depends_on: [db]
    ports:
      - "8101:8069"
    environment:
      HOST: db
      USER: odoo
      PASSWORD: odoo
    volumes:
      - odoo-data:/var/lib/odoo
    command: ["odoo"]
volumes:
  odoo-db:
  odoo-data:
